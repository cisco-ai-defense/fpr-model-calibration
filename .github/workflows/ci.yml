# Copyright 2026 Cisco Systems, Inc. and its affiliates
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ matrix.python-version }}

      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true

      - name: Synchronize the locked environment
        run: uv sync --all-extras --locked

      - name: Lint (ruff check)
        run: uv run ruff check .

      - name: Format check (ruff format --check)
        run: uv run ruff format --check .

      - name: Type check (ty)
        run: uv run ty check src tests scripts

      - name: Unit tests (pytest with coverage)
        run: uv run pytest --cov=fprcal --cov-report=term-missing

      - name: Security audit (pip-audit)
        # --strict would fail on the self editable install; --skip-editable suppresses it.
        run: uv run pip-audit --skip-editable --cache-dir .uv-cache/pip-audit

      - name: License audit (licensecheck)
        run: uv run licensecheck

  package:
    name: Build and inspect distributions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"

      - name: Validate release metadata
        run: python scripts/check_release_policy.py

      - name: Validate pull request release impact
        if: github.event_name == 'pull_request' && github.actor != 'dependabot[bot]'
        env:
          RELEASE_POLICY_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: >-
          python scripts/check_release_policy.py
          --pr-base "$RELEASE_POLICY_BASE_SHA"
          --event-path "$GITHUB_EVENT_PATH"

      - name: Install packaging tools
        run: python -m pip install --upgrade build twine

      - name: Build wheel and source distribution
        run: python -m build

      - name: Validate distribution metadata
        run: python -m twine check --strict dist/*

      - name: Install the wheel in an isolated environment
        run: |
          python -m venv build/package-test
          build/package-test/bin/python -m pip install --upgrade pip
          build/package-test/bin/python -m pip install dist/*.whl
          build/package-test/bin/python -c \
            "import importlib.metadata as m; import fprcal as p; assert p.__version__ == m.version('fprcal')"
